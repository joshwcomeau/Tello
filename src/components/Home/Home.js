import React, { Component } from 'react';

import Button from '../Button';

class Home extends Component {
  render() {
    return (
      <Button color="red">
        Sign into Google
      </Button>
    );
  }
}

export default Home;
